# Backend configuration for Patient 360 Portal

# Connection string for application database. Example formats:
# PostgreSQL: postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DBNAME
# SQLite: sqlite:///./app.db
BACKEND_DB_URL=

# Gemini configuration for chatbot integration
# Set GEMINI_API_KEY to enable real Gemini requests (this implementation uses a demo response if not set)
GEMINI_API_KEY=
# Default model to use for Gemini chat completion
GEMINI_MODEL=gemini-1.5-flash
